import React from 'react';

import './Button.scss';

function Button({children}) {
    return (
        <button className={}>
            {children}
        </button>
    );
}

export default Button;

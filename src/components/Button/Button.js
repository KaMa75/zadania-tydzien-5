import React from 'react';

import './Button.scss';

function Button({children, outlined}) {
    return (
        <button className={}>
            {children}
        </button>
    );
}

export default Button;

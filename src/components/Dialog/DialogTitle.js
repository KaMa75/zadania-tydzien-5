import React from 'react';

function DialogTitle({children}) {
    return (
        <div>
            <h2>
                {children}
            </h2>
        </div>
    );
}

export default DialogTitle;

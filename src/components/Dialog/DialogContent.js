import React from 'react';

function DialogContent({children}) {
    return (
        <div>
            <p>
                {children}
            </p>
        </div>
    );
}

export default DialogContent;
